{
  "name": "myst-anywidget-static-export",
  "version": "0.1.0",
  "description": "MyST plugin that renders anywidget notebook outputs as static, kernel-free interactive widgets",
  "type": "module",
  "private": true,
  "license": "MIT",
  "author": "Development Seed",
  "repository": {
    "type": "git",
    "url": "https://github.com/developmentseed/myst-anywidget-static-export"
  },
  "scripts": {
    "build": "node scripts/build.mjs",
    "typecheck": "tsc --noEmit",
    "test": "npm run build && vitest run",
    "test:watch": "vitest",
    "build:docs": "npm run build && cp dist/plugin.mjs docs/plugin.mjs && cd docs && npx mystmd build --html"
  },
  "devDependencies": {
    "@types/node": "^22.10.5",
    "esbuild": "^0.25.4",
    "typescript": "^5.7.3",
    "vitest": "^2.1.8"
  }
}
